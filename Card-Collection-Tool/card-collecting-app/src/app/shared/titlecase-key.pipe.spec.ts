import { TitlecaseKeyPipe } from './titlecase-key.pipe';

describe('TitlecaseKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new TitlecaseKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
